---
import { patterns } from '../data/patterns';

const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
		<meta name="generator" content={Astro.generator} />
		<title>AI Design Patterns - デザインパターン実験</title>
		<meta name="description" content="AIデザインの様々なパターンを実験・比較するプロジェクト" />
	</head>
	<body>
		<!-- Hero Section -->
		<section class="hero">
			<div class="hero-content">
				<h1 class="title">AI Design Patterns</h1>
				<p class="subtitle">
					5つの異なる美的方向性で実装されたランディングページ<br />
					典型的なAIデザインから独自性のあるデザインまで
				</p>
			</div>
		</section>

		<!-- Patterns Grid -->
		<section class="patterns">
			<div class="container">
				<h2 class="section-title">デザインパターン集</h2>
				<p class="section-description">様々な美的方向性で実装されたランディングページ</p>
				<div class="patterns-grid">
					{patterns.map((pattern) => (
						<a href={`${base}patterns/${pattern.id}`} class="pattern-card">
							<div class="pattern-header">
								<h2>{pattern.title}</h2>
								<p class="pattern-subtitle">{pattern.subtitle}</p>
							</div>
							<p class="pattern-description">{pattern.description}</p>
							<div class="pattern-meta">
								<div class="meta-item">
									<span class="meta-label">トーン:</span>
									<span class="meta-value">{pattern.tone}</span>
								</div>
								<div class="meta-item">
									<span class="meta-label">フォント:</span>
									<span class="meta-value">{pattern.font}</span>
								</div>
							</div>
							<div class="pattern-colors">
								<div class="color-swatch" style={`background: ${pattern.colors.primary}`}></div>
								<div class="color-swatch" style={`background: ${pattern.colors.secondary}`}></div>
								<div class="color-swatch" style={`background: ${pattern.colors.accent}`}></div>
							</div>
							<div class="pattern-tags">
								{pattern.tags.map((tag) => (
									<span class="tag">{tag}</span>
								))}
							</div>
						</a>
					))}
				</div>
			</div>
		</section>

		<!-- Full Site Section -->
		<section class="full-site">
			<div class="container">
				<h2 class="section-title">完全なマルチページサイト</h2>
				<p class="section-description">デザインパターンをベースにした、フル機能のマルチページサイト</p>

				<div class="sites-grid">
					<a href={`${base}organic-site/`} class="site-card">
						<div class="site-hero">
							<div class="site-icon">🌿</div>
							<h3>Organic Site</h3>
						</div>
						<p class="site-description">
							自然な曲線とオーガニックな形状、アースカラーを基調とした優しい配色。<br />
							流れるようなアニメーションとインタラクションで、心地よい体験を提供します。
						</p>
						<div class="site-features">
							<div class="feature">✓ 4ページ構成（Home / About / Services / Contact）</div>
							<div class="feature">✓ スマートフォンレスポンシブ対応</div>
							<div class="feature">✓ スクロールアニメーション実装</div>
						</div>
						<div class="site-cta">サイトを見る →</div>
					</a>

					<a href={`${base}soft-sweet-dreams-site/`} class="site-card">
						<div class="site-hero">
							<div class="site-icon">✨</div>
							<h3>Sweet Dreams Site</h3>
						</div>
						<p class="site-description">
							柔らかなパステルカラーとグラスモーフィズム、夢のような浮遊感のあるデザイン。<br />
							優しいアニメーションで、心地よい体験を創り出します。
						</p>
						<div class="site-features">
							<div class="feature">✓ 4ページ構成（Home / About / Services / Contact）</div>
							<div class="feature">✓ スマートフォンレスポンシブ対応</div>
							<div class="feature">✓ ユーザー動線設計済み</div>
						</div>
						<div class="site-cta">サイトを見る →</div>
					</a>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer class="footer">
			<div class="container">
				<p>
					🤖 このプロジェクトはAIデザインの実験です<br />
					異なる美的方向性を比較し、より良いデザイン方法論を探求します
				</p>
				<p class="footer-link">
					参考: <a href="https://github.com/anthropics/claude-code/blob/main/plugins/frontend-design/skills/frontend-design/SKILL.md" target="_blank" rel="noopener">Claude Code Frontend Design Skill</a>
				</p>
			</div>
		</footer>

		<script>
			if ('serviceWorker' in navigator) {
				window.addEventListener('load', () => {
					const swBase = "{{base}}";
					navigator.serviceWorker
						.register(swBase + "sw.js")
						.catch((err) => console.error("Service worker registration failed", err));
				});
			}
		</script>

		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
				background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
				color: #f8f9fa;
				min-height: 100vh;
			}

			.container {
				max-width: 1400px;
				margin: 0 auto;
				padding: 0 2rem;
			}

			/* Hero Section */
			.hero {
				padding: 6rem 2rem 4rem;
				text-align: center;
			}

			.title {
				font-size: clamp(3rem, 8vw, 5rem);
				font-weight: 900;
				margin-bottom: 1.5rem;
				background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}

			.subtitle {
				font-size: 1.2rem;
				opacity: 0.9;
				line-height: 1.6;
				max-width: 800px;
				margin: 0 auto;
			}

			/* Section Styles */
			.section-title {
				font-size: 2.5rem;
				font-weight: 700;
				text-align: center;
				margin-bottom: 1rem;
			}

			.section-description {
				text-align: center;
				opacity: 0.8;
				margin-bottom: 3rem;
				font-size: 1.1rem;
			}

			/* Patterns Section */
			.patterns {
				padding: 4rem 0 2rem;
			}

			.patterns-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
				gap: 2rem;
			}

			.pattern-card {
				background: rgba(255, 255, 255, 0.05);
				backdrop-filter: blur(10px);
				border: 1px solid rgba(255, 255, 255, 0.1);
				border-radius: 20px;
				padding: 2.5rem;
				text-decoration: none;
				color: inherit;
				transition: all 0.3s;
				display: flex;
				flex-direction: column;
				gap: 1.5rem;
			}

			.pattern-card:hover {
				transform: translateY(-10px);
				background: rgba(255, 255, 255, 0.08);
				border-color: rgba(255, 255, 255, 0.3);
				box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
			}

			.pattern-header h2 {
				font-size: 1.8rem;
				font-weight: 700;
				margin-bottom: 0.5rem;
			}

			.pattern-subtitle {
				opacity: 0.7;
				font-size: 0.95rem;
			}

			.pattern-description {
				line-height: 1.6;
				opacity: 0.9;
				flex-grow: 1;
			}

			.pattern-meta {
				display: flex;
				flex-direction: column;
				gap: 0.5rem;
			}

			.meta-item {
				display: flex;
				gap: 0.5rem;
			}

			.meta-label {
				opacity: 0.6;
				font-size: 0.9rem;
			}

			.meta-value {
				font-weight: 600;
			}

			.pattern-colors {
				display: flex;
				gap: 0.5rem;
			}

			.color-swatch {
				width: 60px;
				height: 60px;
				border-radius: 10px;
				border: 2px solid rgba(255, 255, 255, 0.2);
			}

			.pattern-tags {
				display: flex;
				flex-wrap: wrap;
				gap: 0.5rem;
			}

			.tag {
				background: rgba(255, 255, 255, 0.1);
				padding: 0.4rem 0.8rem;
				border-radius: 20px;
				font-size: 0.85rem;
				border: 1px solid rgba(255, 255, 255, 0.1);
			}

			/* Full Site Section */
			.full-site {
				padding: 4rem 0 6rem;
			}

			.sites-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(450px, 1fr));
				gap: 2.5rem;
			}

			.site-card {
				background: rgba(255, 255, 255, 0.05);
				backdrop-filter: blur(10px);
				border: 1px solid rgba(255, 255, 255, 0.1);
				border-radius: 24px;
				padding: 3rem;
				text-decoration: none;
				color: inherit;
				transition: all 0.4s;
				display: flex;
				flex-direction: column;
			}

			.site-card:hover {
				transform: translateY(-8px) scale(1.01);
				background: rgba(255, 255, 255, 0.08);
				box-shadow: 0 30px 80px rgba(102, 126, 234, 0.2);
			}

			.site-card:first-child:hover {
				border-color: rgba(106, 153, 78, 0.5);
				box-shadow: 0 30px 80px rgba(106, 153, 78, 0.2);
			}

			.site-card:last-child:hover {
				border-color: rgba(192, 132, 252, 0.5);
				box-shadow: 0 30px 80px rgba(192, 132, 252, 0.2);
			}

			.site-hero {
				display: flex;
				align-items: center;
				gap: 1.5rem;
				margin-bottom: 2rem;
			}

			.site-icon {
				font-size: 4rem;
				line-height: 1;
			}

			.site-card:first-child .site-hero h3 {
				font-size: 2.5rem;
				font-weight: 700;
				background: linear-gradient(135deg, #81b29a 0%, #6a994e 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}

			.site-card:last-child .site-hero h3 {
				font-size: 2.5rem;
				font-weight: 700;
				background: linear-gradient(135deg, #C084FC 0%, #F9A8D4 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}

			.site-description {
				line-height: 1.8;
				opacity: 0.9;
				margin-bottom: 2rem;
				font-size: 1.1rem;
			}

			.site-features {
				display: flex;
				flex-direction: column;
				gap: 0.8rem;
				margin-bottom: 2rem;
			}

			.feature {
				font-size: 1rem;
				opacity: 0.85;
				padding-left: 0.5rem;
			}

			.site-cta {
				display: inline-block;
				color: white;
				padding: 1rem 2.5rem;
				border-radius: 50px;
				font-weight: 600;
				font-size: 1.1rem;
				transition: all 0.3s;
			}

			.site-card:first-child .site-cta {
				background: linear-gradient(135deg, #81b29a 0%, #6a994e 100%);
			}

			.site-card:last-child .site-cta {
				background: linear-gradient(135deg, #C084FC 0%, #F9A8D4 100%);
			}

			.site-card:first-child:hover .site-cta {
				transform: translateX(10px);
				box-shadow: 0 10px 30px rgba(129, 178, 154, 0.4);
			}

			.site-card:last-child:hover .site-cta {
				transform: translateX(10px);
				box-shadow: 0 10px 30px rgba(192, 132, 252, 0.4);
			}

			/* Footer */
			.footer {
				padding: 3rem 0;
				text-align: center;
				border-top: 1px solid rgba(255, 255, 255, 0.1);
			}

			.footer p {
				opacity: 0.8;
				line-height: 1.8;
				margin-bottom: 1rem;
			}

			.footer-link a {
				color: #4ecdc4;
				text-decoration: none;
			}

			.footer-link a:hover {
				text-decoration: underline;
			}

			/* Responsive */
			@media (max-width: 768px) {
				.hero {
					padding: 4rem 1.5rem 3rem;
				}

				.title {
					font-size: clamp(2rem, 10vw, 3rem);
				}

				.subtitle {
					font-size: 1rem;
				}

				.section-title {
					font-size: 2rem;
				}

				.patterns-grid {
					grid-template-columns: 1fr;
				}

				.sites-grid {
					grid-template-columns: 1fr;
				}

				.site-card {
					padding: 2rem;
				}

				.site-hero {
					flex-direction: column;
					align-items: flex-start;
					gap: 1rem;
				}

				.site-card:first-child .site-hero h3,
				.site-card:last-child .site-hero h3 {
					font-size: 2rem;
				}

				.site-icon {
					font-size: 3rem;
				}

				.site-description {
					font-size: 1rem;
				}
			}
		</style>
	</body>
</html>
