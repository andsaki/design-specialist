---
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
		<meta name="generator" content={Astro.generator} />
		<title>AI Generated Design - 典型的なAIデザインの実験</title>
		<meta name="description" content="AIが生成する典型的なデザインパターンの研究・実験LP" />
	</head>
	<body>
		<!-- Navigation -->
		<nav class="nav">
			<div class="nav-container">
				<div class="logo">AI Design Experiment</div>
				<ul class="nav-menu" id="navMenu">
					<li><a href="#patterns">パターン</a></li>
					<li><a href="#analysis">分析</a></li>
					<li><a href="#methodology">方法論</a></li>
				</ul>
				<button class="menu-toggle" id="menuToggle" aria-label="メニュー">
					<span></span>
					<span></span>
					<span></span>
				</button>
			</div>
		</nav>

		<!-- Hero Section -->
		<section class="hero">
			<div class="hero-background">
				<div class="gradient-orb orb-1"></div>
				<div class="gradient-orb orb-2"></div>
				<div class="gradient-orb orb-3"></div>
			</div>
			<div class="hero-content">
				<div class="experiment-badge">🤖 AI Generated Design Experiment</div>
				<h1 class="hero-title">
					<span class="title-line">This is a</span>
					<span class="title-line gradient-text">Generic</span>
					<span class="title-line">AI Design</span>
				</h1>
				<p class="hero-subtitle">
					典型的なAI生成デザインの実験LP<br />
					紫グラデーション、システムフォント、予測可能なレイアウト
				</p>
				<div class="hero-buttons">
					<a href="#patterns" class="btn btn-primary">パターンを見る</a>
					<a href="#methodology" class="btn btn-secondary">方法論</a>
				</div>
			</div>
			<div class="scroll-indicator">
				<span>Scroll</span>
				<div class="scroll-line"></div>
			</div>
		</section>

		<!-- Patterns Section -->
		<section id="patterns" class="features">
			<div class="container">
				<h2 class="section-title">典型的なAIデザインパターン</h2>
				<p class="section-description">このLPで使用されている「避けるべき」デザイン要素</p>
				<div class="features-grid">
					<div class="feature-card">
						<div class="feature-icon">
							<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<circle cx="12" cy="12" r="10"></circle>
								<circle cx="12" cy="12" r="6"></circle>
								<circle cx="12" cy="12" r="2"></circle>
							</svg>
						</div>
						<h3>❌ 紫グラデーション背景</h3>
						<p>AI生成デザインで最も多用される配色パターン。白背景上の紫グラデーションは陳腐化している</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M4 7V4h16v3M9 20h6M12 4v16"></path>
							</svg>
						</div>
						<h3>❌ システムフォント</h3>
						<p>Inter、Roboto、-apple-systemなど。独自性に欠け、どのサイトも似たような印象になる</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<rect x="3" y="3" width="18" height="18" rx="2"></rect>
								<path d="M3 9h18M9 21V9"></path>
							</svg>
						</div>
						<h3>❌ 予測可能なレイアウト</h3>
						<p>ヒーロー → 特徴 → About → コンタクトという定型パターン。記憶に残らない</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Analysis Section -->
		<section id="analysis" class="about">
			<div class="container">
				<div class="about-content">
					<div class="about-text">
						<h2 class="section-title">なぜこれが<br />問題なのか</h2>
						<p class="about-description">
							AIが生成するデザインは、トレーニングデータの平均値に収束しがちです。
							その結果、以下のような問題が発生します：
						</p>
						<ul class="analysis-list">
							<li><strong>同質化</strong>: すべてのサイトが似たような見た目になる</li>
							<li><strong>記憶に残らない</strong>: 独自性がなく、ブランド認知が困難</li>
							<li><strong>トレンドの陳腐化</strong>: Space Groteskなど流行フォントへの過度な依存</li>
							<li><strong>意図性の欠如</strong>: 明確な美的方向性がない</li>
						</ul>
						<p class="about-description">
							このLPは、これらの問題を可視化するための実験です。
						</p>
						<a href="#methodology" class="btn btn-primary">方法論を見る</a>
					</div>
					<div class="about-visual">
						<div class="visual-grid">
							<div class="visual-item item-1"></div>
							<div class="visual-item item-2"></div>
							<div class="visual-item item-3"></div>
							<div class="visual-item item-4"></div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Methodology Section -->
		<section id="methodology" class="contact">
			<div class="container">
				<div class="contact-content">
					<h2 class="section-title">デザイン<br />方法論</h2>
					<p class="contact-description">
						独自性のあるプロダクショングレードのデザインを実現するために、以下の方法論を採用しています：
					</p>
					<div class="methodology-list">
						<div class="methodology-item">
							<h3>1. Purpose（目的）</h3>
							<p>インターフェースが解決する問題とユーザーを明確にする</p>
						</div>
						<div class="methodology-item">
							<h3>2. Tone（トーン）</h3>
							<p>極端な美的方向性を選び、コミットする（ミニマリズム、マキシマリズム、レトロフューチャーなど）</p>
						</div>
						<div class="methodology-item">
							<h3>3. Constraints（制約）</h3>
							<p>技術要件、パフォーマンス、アクセシビリティを考慮する</p>
						</div>
						<div class="methodology-item">
							<h3>4. Differentiation（差別化）</h3>
							<p>記憶に残る1つの要素を見つける</p>
						</div>
					</div>
					<p class="contact-description">
						<strong>参考資料:</strong><br />
						<a href="https://github.com/anthropics/claude-code/blob/main/plugins/frontend-design/skills/frontend-design/SKILL.md" target="_blank" rel="noopener" class="resource-link">Claude Code Frontend Design Skill</a>
					</p>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer class="footer">
			<div class="container">
				<div class="footer-content">
					<div class="logo">AI Design Experiment</div>
					<p>
						🤖 このLPは100% AIによって生成されたデザインの実験です<br />
						典型的なAIデザインパターンを可視化し、より良いデザイン方法論を探求します
					</p>
					<p>&copy; 2026 AI Design Experiment. All rights reserved.</p>
				</div>
			</div>
		</footer>

		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			:root {
				--primary: #ff6b6b;
				--secondary: #4ecdc4;
				--accent: #ffe66d;
				--dark: #1a1a2e;
				--light: #f8f9fa;
				--gradient-1: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				--gradient-2: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
				--gradient-3: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
			}

			body {
				font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
				background-color: var(--dark);
				color: var(--light);
				overflow-x: hidden;
			}

			.container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 2rem;
			}

			/* Navigation */
			.nav {
				position: fixed;
				top: 0;
				left: 0;
				right: 0;
				z-index: 1000;
				background: rgba(26, 26, 46, 0.8);
				backdrop-filter: blur(10px);
				border-bottom: 1px solid rgba(255, 255, 255, 0.1);
			}

			.nav-container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 1.5rem 2rem;
				display: flex;
				justify-content: space-between;
				align-items: center;
			}

			.logo {
				font-size: 1.5rem;
				font-weight: 700;
				background: var(--gradient-1);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}

			.nav-menu {
				display: flex;
				gap: 2rem;
				list-style: none;
				flex-direction: row;
				position: static;
				width: auto;
				height: auto;
				background: transparent;
				padding: 0;
				align-items: center;
			}

			.nav-menu li {
				display: inline-block;
			}

			.nav-menu a {
				color: var(--light);
				text-decoration: none;
				transition: color 0.3s;
				white-space: nowrap;
				writing-mode: horizontal-tb;
				display: inline-block;
			}

			.nav-menu a:hover {
				color: var(--secondary);
			}

			.menu-toggle {
				display: none;
				flex-direction: column;
				justify-content: center;
				align-items: center;
				gap: 5px;
				background: none;
				border: none;
				cursor: pointer;
				padding: 0.5rem;
				width: 40px;
				height: 40px;
				position: relative;
			}

			.menu-toggle span {
				width: 25px;
				height: 2px;
				background: var(--light);
				border-radius: 2px;
				transition: all 0.3s ease;
				display: block;
			}

			.menu-toggle.active {
				gap: 0;
			}

			.menu-toggle.active span {
				position: absolute;
			}

			.menu-toggle.active span:nth-child(1) {
				transform: rotate(45deg);
			}

			.menu-toggle.active span:nth-child(2) {
				opacity: 0;
			}

			.menu-toggle.active span:nth-child(3) {
				transform: rotate(-45deg);
			}

			.cta-button {
				background: var(--gradient-1);
				padding: 0.5rem 1.5rem;
				border-radius: 25px;
				color: white !important;
			}

			/* Hero Section */
			.hero {
				position: relative;
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				overflow: hidden;
			}

			.hero-background {
				position: absolute;
				inset: 0;
				z-index: 0;
			}

			.gradient-orb {
				position: absolute;
				border-radius: 50%;
				filter: blur(80px);
				opacity: 0.6;
				animation: float 20s infinite ease-in-out;
			}

			.orb-1 {
				width: 500px;
				height: 500px;
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				top: -10%;
				left: -10%;
				animation-delay: 0s;
			}

			.orb-2 {
				width: 400px;
				height: 400px;
				background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
				bottom: -10%;
				right: -10%;
				animation-delay: -7s;
			}

			.orb-3 {
				width: 300px;
				height: 300px;
				background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
				animation-delay: -14s;
			}

			@keyframes float {
				0%, 100% {
					transform: translate(0, 0) scale(1);
				}
				33% {
					transform: translate(30px, -50px) scale(1.1);
				}
				66% {
					transform: translate(-20px, 20px) scale(0.9);
				}
			}

			.hero-content {
				position: relative;
				z-index: 1;
				text-align: center;
				padding: 2rem;
			}

			.experiment-badge {
				display: inline-block;
				background: rgba(255, 255, 255, 0.1);
				backdrop-filter: blur(10px);
				border: 1px solid rgba(255, 255, 255, 0.2);
				padding: 0.5rem 1.5rem;
				border-radius: 50px;
				margin-bottom: 2rem;
				font-size: 0.9rem;
				font-weight: 600;
				letter-spacing: 0.05em;
			}

			.hero-title {
				font-size: clamp(3rem, 10vw, 6rem);
				font-weight: 900;
				line-height: 1.1;
				margin-bottom: 1.5rem;
			}

			.title-line {
				display: block;
			}

			.gradient-text {
				background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				animation: gradientShift 5s ease infinite;
				background-size: 200% 200%;
			}

			@keyframes gradientShift {
				0%, 100% {
					background-position: 0% 50%;
				}
				50% {
					background-position: 100% 50%;
				}
			}

			.hero-subtitle {
				font-size: 1.2rem;
				margin-bottom: 3rem;
				opacity: 0.9;
				line-height: 1.6;
			}

			.hero-buttons {
				display: flex;
				gap: 1rem;
				justify-content: center;
				flex-wrap: wrap;
			}

			.btn {
				padding: 1rem 2.5rem;
				border-radius: 50px;
				text-decoration: none;
				font-weight: 600;
				transition: all 0.3s;
				display: inline-block;
				border: none;
				cursor: pointer;
				font-size: 1rem;
			}

			.btn-primary {
				background: var(--gradient-1);
				color: white;
			}

			.btn-primary:hover {
				transform: translateY(-2px);
				box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
			}

			.btn-secondary {
				background: transparent;
				color: white;
				border: 2px solid white;
			}

			.btn-secondary:hover {
				background: white;
				color: var(--dark);
			}

			.btn-full {
				width: 100%;
			}

			.scroll-indicator {
				position: absolute;
				bottom: 2rem;
				left: 50%;
				transform: translateX(-50%);
				display: flex;
				flex-direction: column;
				align-items: center;
				gap: 0.5rem;
				opacity: 0.6;
			}

			.scroll-line {
				width: 2px;
				height: 50px;
				background: linear-gradient(to bottom, var(--secondary), transparent);
				animation: scrollLine 2s infinite;
			}

			@keyframes scrollLine {
				0% {
					transform: translateY(0);
					opacity: 0;
				}
				50% {
					opacity: 1;
				}
				100% {
					transform: translateY(20px);
					opacity: 0;
				}
			}

			/* Features Section */
			.features {
				padding: 8rem 0;
				background: linear-gradient(180deg, var(--dark) 0%, #16213e 100%);
			}

			.section-title {
				font-size: clamp(2.5rem, 6vw, 4rem);
				font-weight: 900;
				margin-bottom: 4rem;
				text-align: center;
			}

			.section-description {
				text-align: center;
				font-size: 1.1rem;
				opacity: 0.8;
				margin-bottom: 3rem;
				max-width: 800px;
				margin-left: auto;
				margin-right: auto;
			}

			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 2rem;
			}

			.feature-card {
				background: rgba(255, 255, 255, 0.05);
				backdrop-filter: blur(10px);
				border: 1px solid rgba(255, 255, 255, 0.1);
				border-radius: 20px;
				padding: 3rem 2rem;
				transition: all 0.3s;
			}

			.feature-card:hover {
				transform: translateY(-10px);
				background: rgba(255, 255, 255, 0.08);
				border-color: var(--secondary);
			}

			.feature-icon {
				width: 80px;
				height: 80px;
				background: var(--gradient-1);
				border-radius: 20px;
				display: flex;
				align-items: center;
				justify-content: center;
				margin-bottom: 1.5rem;
				color: white;
			}

			.feature-card h3 {
				font-size: 1.5rem;
				margin-bottom: 1rem;
			}

			.feature-card p {
				opacity: 0.8;
				line-height: 1.6;
			}

			/* About Section */
			.about {
				padding: 8rem 0;
				background: var(--dark);
			}

			.about-content {
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 4rem;
				align-items: center;
			}

			.about-text .section-title {
				text-align: left;
				margin-bottom: 2rem;
			}

			.about-description {
				font-size: 1.1rem;
				line-height: 1.8;
				opacity: 0.9;
				margin-bottom: 1.5rem;
			}

			.analysis-list {
				list-style: none;
				margin: 2rem 0;
			}

			.analysis-list li {
				padding: 1rem 0;
				border-left: 3px solid var(--secondary);
				padding-left: 1.5rem;
				margin-bottom: 1rem;
				opacity: 0.9;
				line-height: 1.6;
			}

			.analysis-list strong {
				color: var(--secondary);
				display: block;
				margin-bottom: 0.3rem;
			}

			.about-visual {
				position: relative;
				height: 500px;
			}

			.visual-grid {
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 1rem;
				height: 100%;
			}

			.visual-item {
				border-radius: 20px;
				background: var(--gradient-1);
				transition: all 0.3s;
			}

			.visual-item:nth-child(1) {
				background: var(--gradient-1);
			}

			.visual-item:nth-child(2) {
				background: var(--gradient-2);
				transform: translateY(2rem);
			}

			.visual-item:nth-child(3) {
				background: var(--gradient-3);
				transform: translateY(-2rem);
			}

			.visual-item:nth-child(4) {
				background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
			}

			.visual-item:hover {
				transform: scale(1.05);
			}

			/* Contact Section */
			.contact {
				padding: 8rem 0;
				background: linear-gradient(180deg, var(--dark) 0%, #0f3460 100%);
			}

			.contact-content {
				max-width: 600px;
				margin: 0 auto;
				text-align: center;
			}

			.contact-description {
				font-size: 1.1rem;
				opacity: 0.9;
				margin-bottom: 3rem;
				line-height: 1.6;
			}

			.resource-link {
				color: var(--secondary);
				text-decoration: underline;
				transition: opacity 0.3s;
			}

			.resource-link:hover {
				opacity: 0.8;
			}

			.methodology-list {
				display: flex;
				flex-direction: column;
				gap: 2rem;
				margin: 3rem 0;
			}

			.methodology-item {
				background: rgba(255, 255, 255, 0.05);
				border: 1px solid rgba(255, 255, 255, 0.1);
				border-radius: 15px;
				padding: 2rem;
				transition: all 0.3s;
			}

			.methodology-item:hover {
				background: rgba(255, 255, 255, 0.08);
				border-color: var(--secondary);
			}

			.methodology-item h3 {
				font-size: 1.3rem;
				margin-bottom: 0.5rem;
				color: var(--secondary);
			}

			.methodology-item p {
				opacity: 0.9;
				line-height: 1.6;
			}

			.contact-form {
				display: flex;
				flex-direction: column;
				gap: 1.5rem;
			}

			.form-group input,
			.form-group textarea {
				width: 100%;
				padding: 1.2rem;
				border-radius: 15px;
				border: 1px solid rgba(255, 255, 255, 0.2);
				background: rgba(255, 255, 255, 0.05);
				color: white;
				font-size: 1rem;
				transition: all 0.3s;
			}

			.form-group input:focus,
			.form-group textarea:focus {
				outline: none;
				border-color: var(--secondary);
				background: rgba(255, 255, 255, 0.08);
			}

			.form-group input::placeholder,
			.form-group textarea::placeholder {
				color: rgba(255, 255, 255, 0.5);
			}

			/* Footer */
			.footer {
				padding: 3rem 0;
				background: #0a0e27;
				border-top: 1px solid rgba(255, 255, 255, 0.1);
			}

			.footer-content {
				text-align: center;
			}

			.footer-content p {
				margin-top: 1rem;
				opacity: 0.6;
			}

			/* Responsive */
			@media (max-width: 968px) {
				.container {
					padding: 0 2rem;
				}

				.about-content {
					grid-template-columns: 1fr;
				}

				.about-visual {
					height: 400px;
					max-width: 600px;
					margin: 0 auto;
				}

				.visual-item:nth-child(2),
				.visual-item:nth-child(3) {
					transform: translateY(0);
				}
			}

			@media (max-width: 768px) {
				.container {
					padding: 0 1.5rem;
				}

				.nav-container {
					padding: 1.2rem 1.5rem;
				}

				.menu-toggle {
					display: flex;
					z-index: 1001;
				}

				.nav-menu {
					position: fixed;
					top: 70px;
					right: 0;
					width: 250px;
					height: calc(100vh - 70px);
					background: rgba(26, 26, 46, 0.98);
					backdrop-filter: blur(10px);
					flex-direction: column;
					padding: 2rem;
					gap: 0;
					transform: translateX(100%);
					transition: transform 0.3s ease;
					border-left: 1px solid rgba(255, 255, 255, 0.1);
					z-index: 1000;
				}

				.nav-menu.active {
					transform: translateX(0);
				}

				.nav-menu li {
					margin-bottom: 1.5rem;
				}

				.nav-menu a {
					font-size: 1.1rem;
					display: block;
				}

				.cta-button {
					padding: 0.4rem 1.2rem;
					font-size: 0.9rem;
				}

				.hero {
					min-height: 80vh;
				}

				.hero-content {
					padding: 1.5rem;
				}

				.hero-title {
					font-size: clamp(2.5rem, 8vw, 5rem);
				}

				.hero-subtitle {
					font-size: 1.1rem;
				}

				.cta-buttons {
					flex-direction: column;
					width: 100%;
				}

				.cta-buttons .button {
					width: 100%;
					justify-content: center;
				}

				.about {
					padding: 5rem 0;
				}

				.section-title {
					font-size: 2.5rem;
				}

				.about-visual {
					height: 400px;
				}

				.features {
					padding: 5rem 0;
				}

				.features-grid {
					grid-template-columns: 1fr;
				}

				.methodology {
					padding: 5rem 0;
				}

				.why-section {
					padding: 5rem 0;
				}

				.footer {
					padding: 3rem 1.5rem;
				}

				.footer-content {
					gap: 3rem;
				}

				.footer-section h3 {
					font-size: 1.1rem;
				}
			}

			@media (max-width: 480px) {
				.container {
					padding: 0 1rem;
				}

				.nav-container {
					padding: 1rem;
				}

				.nav-menu {
					width: 100%;
					padding: 1.5rem 1rem;
				}

				.hero-content {
					padding: 1rem;
				}

				.experiment-badge {
					font-size: 0.75rem;
					padding: 0.4rem 1rem;
				}

				.hero-title {
					font-size: clamp(2rem, 10vw, 3.5rem);
				}

				.hero-subtitle {
					font-size: 0.95rem;
					margin-bottom: 2rem;
				}

				.btn {
					padding: 0.9rem 2rem;
					font-size: 0.95rem;
				}

				.about {
					padding: 3rem 0;
				}

				.section-title {
					font-size: 1.8rem;
					margin-bottom: 2rem;
				}

				.section-description {
					font-size: 0.95rem;
					margin-bottom: 2rem;
				}

				.about-description {
					font-size: 0.95rem;
				}

				.about-visual {
					height: 350px;
					max-width: 100%;
				}

				.features {
					padding: 3rem 0;
				}

				.feature-card {
					padding: 1.5rem 1rem;
				}

				.feature-icon {
					width: 60px;
					height: 60px;
					margin-bottom: 1rem;
				}

				.feature-card h3 {
					font-size: 1.2rem;
				}

				.feature-card p {
					font-size: 0.95rem;
				}

				.contact {
					padding: 3rem 0;
				}

				.contact-description {
					font-size: 0.95rem;
				}

				.methodology-item {
					padding: 1.5rem;
				}

				.methodology-item h3 {
					font-size: 1.1rem;
				}

				.methodology-item p {
					font-size: 0.95rem;
				}

				.footer {
					padding: 2rem 1rem;
				}

				.footer-content p {
					font-size: 0.9rem;
				}
			}
		</style>

		<script>
			// Mobile menu toggle
			const menuToggle = document.getElementById('menuToggle');
			const navMenu = document.getElementById('navMenu');

			if (menuToggle && navMenu) {
				menuToggle.addEventListener('click', () => {
					menuToggle.classList.toggle('active');
					navMenu.classList.toggle('active');
				});

				// Close menu when clicking on a link
				navMenu.querySelectorAll('a').forEach(link => {
					link.addEventListener('click', () => {
						menuToggle.classList.remove('active');
						navMenu.classList.remove('active');
					});
				});

				// Close menu when clicking outside
				document.addEventListener('click', (e) => {
					const target = e.target as Node;
					if (!navMenu.contains(target) && !menuToggle.contains(target)) {
						menuToggle.classList.remove('active');
						navMenu.classList.remove('active');
					}
				});
			}

			// Smooth scrolling
			document.querySelectorAll('a[href^="#"]').forEach(anchor => {
				anchor.addEventListener('click', (e) => {
					e.preventDefault();
					const href = (anchor as HTMLAnchorElement).getAttribute('href');
					if (href) {
						const target = document.querySelector(href);
						if (target) {
							target.scrollIntoView({
								behavior: 'smooth',
								block: 'start'
							});
						}
					}
				});
			});

			// Intersection Observer for animations
			const observerOptions = {
				threshold: 0.1,
				rootMargin: '0px 0px -100px 0px'
			};

			const observer = new IntersectionObserver((entries) => {
				entries.forEach(entry => {
					if (entry.isIntersecting) {
						const element = entry.target as HTMLElement;
						element.style.opacity = '1';
						element.style.transform = 'translateY(0)';
					}
				});
			}, observerOptions);

			// Observe all sections
			document.querySelectorAll('section').forEach(section => {
				const el = section as HTMLElement;
				el.style.opacity = '0';
				el.style.transform = 'translateY(50px)';
				el.style.transition = 'opacity 0.6s, transform 0.6s';
				observer.observe(section);
			});

			// Show hero immediately
			const hero = document.querySelector('.hero') as HTMLElement | null;
			if (hero) {
				hero.style.opacity = '1';
				hero.style.transform = 'translateY(0)';
			}
		</script>
	</body>
</html>
