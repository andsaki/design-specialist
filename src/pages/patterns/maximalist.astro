---
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>MAXIMALIST CHAOS - MORE IS MORE!</title>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Bangers&family=Righteous&family=Black+Ops+One&family=Rubik+Mono+One&family=Monoton&display=swap" rel="stylesheet">
	</head>
	<body>
		<!-- Chaotic Background Layers -->
		<div class="bg-layer layer-1"></div>
		<div class="bg-layer layer-2"></div>
		<div class="bg-layer layer-3"></div>
		<div class="noise-overlay"></div>

		<!-- Navigation -->
		<nav class="navbar">
			<a href={base} class="nav-link">← BACK</a>
			<div class="nav-decorations">
				<span class="decoration">★</span>
				<span class="decoration">●</span>
				<span class="decoration">■</span>
			</div>
		</nav>

		<!-- Hero Section -->
		<section class="hero">
			<div class="hero-chaos">
				<div class="chaos-shape shape-1">✦</div>
				<div class="chaos-shape shape-2">◆</div>
				<div class="chaos-shape shape-3">●</div>
				<div class="chaos-shape shape-4">★</div>
				<div class="chaos-shape shape-5">▲</div>
			</div>

			<div class="container">
				<div class="sticker-collection">
					<div class="sticker sticker-1">WOW!</div>
					<div class="sticker sticker-2">BOLD</div>
					<div class="sticker sticker-3">CHAOS</div>
				</div>

				<h1 class="main-title">
					<span class="title-line line-1">MAXIMAL</span>
					<span class="title-line line-2">IST</span>
					<span class="title-line line-3">CHAOS!</span>
				</h1>

				<div class="subtitle-container">
					<p class="subtitle">
						<span class="highlight-pink">MORE</span> IS <span class="highlight-yellow">MORE</span>!
						<span class="highlight-blue">EVERYTHING</span> AT ONCE!
					</p>
				</div>

				<div class="cta-cluster">
					<button class="btn btn-1">CLICK ME!</button>
					<button class="btn btn-2">NO, ME!</button>
					<button class="btn btn-3">ALSO ME!</button>
				</div>
			</div>
		</section>

		<!-- Features Grid -->
		<section class="features">
			<div class="container">
				<h2 class="section-title">
					<span class="title-word word-1">TOO</span>
					<span class="title-word word-2">MUCH</span>
					<span class="title-word word-3">IS</span>
					<span class="title-word word-4">PERFECT!</span>
				</h2>

				<div class="features-chaos">
					<div class="feature-box box-1">
						<div class="box-header">001</div>
						<h3>COLORS EVERYWHERE</h3>
						<p>Why choose one when you can have ALL OF THEM at the same time?!</p>
						<div class="box-badge">★★★</div>
					</div>

					<div class="feature-box box-2">
						<div class="box-header">002</div>
						<h3>LAYER UPON LAYER</h3>
						<p>Stack it! Overlap it! More layers means more IMPACT!</p>
						<div class="box-badge">●●●</div>
					</div>

					<div class="feature-box box-3">
						<div class="box-header">003</div>
						<h3>MIXED FONTS</h3>
						<p>Different typefaces fighting for attention - CHAOS IS BEAUTIFUL!</p>
						<div class="box-badge">■■■</div>
					</div>

					<div class="feature-box box-4">
						<div class="box-header">004</div>
						<h3>VISUAL OVERLOAD</h3>
						<p>Intentional saturation! Your eyes don't know where to look - PERFECT!</p>
						<div class="box-badge">▲▲▲</div>
					</div>

					<div class="feature-box box-5">
						<div class="box-header">005</div>
						<h3>PATTERNS CLASH</h3>
						<p>Stripes! Dots! Gradients! Mix them ALL together!</p>
						<div class="box-badge">◆◆◆</div>
					</div>

					<div class="feature-box box-6">
						<div class="box-header">006</div>
						<h3>NO WHITE SPACE</h3>
						<p>Empty space? We don't know her! FILL EVERYTHING!</p>
						<div class="box-badge">✦✦✦</div>
					</div>
				</div>
			</div>
		</section>

		<!-- CTA Section -->
		<section class="cta">
			<div class="cta-background"></div>
			<div class="container">
				<div class="cta-content">
					<div class="cta-badges">
						<span class="badge badge-1">NEW!</span>
						<span class="badge badge-2">HOT!</span>
						<span class="badge badge-3">WOW!</span>
					</div>
					<h2 class="cta-title">
						READY FOR<br/>
						<span class="cta-emphasis">MAXIMUM CHAOS?!</span>
					</h2>
					<p class="cta-text">
						Join the revolution of MORE! Why be minimal when you can be MAXIMAL?!
					</p>
					<button class="cta-button">
						LET'S GO! →
						<div class="button-effects"></div>
					</button>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer class="footer">
			<div class="footer-pattern"></div>
			<p class="footer-text">
				MAXIMALIST CHAOS © 2025 | MORE IS MORE | EVERYTHING EVERYWHERE ALL AT ONCE
			</p>
		</footer>

		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			:root {
				--pink: #ff006e;
				--yellow: #ffbe0b;
				--blue: #3a86ff;
				--cyan: #00f5ff;
				--purple: #8338ec;
				--orange: #fb5607;
			}

			body {
				font-family: 'Bebas Neue', sans-serif;
				background: #000;
				color: #fff;
				overflow-x: hidden;
				position: relative;
			}

			.container {
				max-width: 1400px;
				margin: 0 auto;
				padding: 0 2rem;
			}

			/* Background Chaos */
			.bg-layer {
				position: fixed;
				inset: 0;
				z-index: -2;
			}

			.layer-1 {
				background:
					linear-gradient(45deg, var(--pink) 0%, var(--yellow) 50%, var(--blue) 100%);
				opacity: 0.8;
			}

			.layer-2 {
				background:
					repeating-linear-gradient(45deg, transparent, transparent 10px, var(--cyan) 10px, var(--cyan) 20px),
					repeating-linear-gradient(-45deg, transparent, transparent 10px, var(--purple) 10px, var(--purple) 20px);
				opacity: 0.2;
				animation: slide 10s linear infinite;
			}

			.layer-3 {
				background:
					radial-gradient(circle at 20% 50%, var(--orange) 0%, transparent 50%),
					radial-gradient(circle at 80% 50%, var(--cyan) 0%, transparent 50%);
				animation: pulse 5s ease-in-out infinite;
			}

			@keyframes slide {
				from { background-position: 0 0, 0 0; }
				to { background-position: 100px 100px, -100px -100px; }
			}

			@keyframes pulse {
				0%, 100% { opacity: 0.3; }
				50% { opacity: 0.6; }
			}

			.noise-overlay {
				position: fixed;
				inset: 0;
				background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
				opacity: 0.15;
				z-index: -1;
			}

			/* Navigation */
			.navbar {
				display: flex;
				justify-content: space-between;
				align-items: center;
				padding: 2rem;
			}

			.nav-link {
				font-size: 1.5rem;
				color: var(--yellow);
				text-decoration: none;
				background: var(--pink);
				padding: 0.5rem 2rem;
				transform: rotate(-2deg);
				border: 4px solid #000;
				box-shadow: 6px 6px 0 var(--blue);
				transition: all 0.2s;
			}

			.nav-link:hover {
				transform: rotate(2deg) scale(1.1);
			}

			.nav-decorations {
				display: flex;
				gap: 1rem;
			}

			.decoration {
				font-size: 2rem;
				animation: spin 3s linear infinite;
			}

			.decoration:nth-child(1) { color: var(--pink); animation-delay: 0s; }
			.decoration:nth-child(2) { color: var(--yellow); animation-delay: 0.5s; }
			.decoration:nth-child(3) { color: var(--blue); animation-delay: 1s; }

			@keyframes spin {
				from { transform: rotate(0deg); }
				to { transform: rotate(360deg); }
			}

			/* Hero Section */
			.hero {
				padding: 4rem 0 8rem;
				position: relative;
				text-align: center;
			}

			.hero-chaos {
				position: absolute;
				inset: 0;
				overflow: hidden;
			}

			.chaos-shape {
				position: absolute;
				font-size: 4rem;
				animation: chaosFloat 5s ease-in-out infinite;
				opacity: 0.6;
			}

			.shape-1 { top: 10%; left: 10%; color: var(--pink); animation-delay: 0s; }
			.shape-2 { top: 20%; right: 15%; color: var(--yellow); animation-delay: 1s; }
			.shape-3 { bottom: 30%; left: 20%; color: var(--blue); animation-delay: 2s; }
			.shape-4 { top: 50%; right: 10%; color: var(--cyan); animation-delay: 3s; }
			.shape-5 { bottom: 20%; right: 30%; color: var(--orange); animation-delay: 4s; }

			@keyframes chaosFloat {
				0%, 100% { transform: translate(0, 0) rotate(0deg); }
				25% { transform: translate(20px, -20px) rotate(90deg); }
				50% { transform: translate(-20px, 20px) rotate(180deg); }
				75% { transform: translate(20px, 20px) rotate(270deg); }
			}

			.sticker-collection {
				display: flex;
				justify-content: center;
				gap: 2rem;
				margin-bottom: 2rem;
			}

			.sticker {
				font-family: 'Bangers', cursive;
				font-size: 2rem;
				padding: 1rem 2rem;
				transform: rotate(-5deg);
				border: 4px solid #000;
				box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.5);
			}

			.sticker-1 { background: var(--pink); color: var(--yellow); }
			.sticker-2 { background: var(--yellow); color: var(--pink); transform: rotate(5deg); }
			.sticker-3 { background: var(--blue); color: var(--yellow); }

			.main-title {
				position: relative;
				z-index: 1;
				margin-bottom: 2rem;
			}

			.title-line {
				display: block;
				font-size: clamp(4rem, 15vw, 10rem);
				font-weight: 900;
				line-height: 0.9;
				text-shadow: 6px 6px 0 rgba(0, 0, 0, 0.8);
			}

			.line-1 {
				font-family: 'Bebas Neue', sans-serif;
				color: var(--pink);
				transform: rotate(-3deg);
			}

			.line-2 {
				font-family: 'Righteous', cursive;
				color: var(--yellow);
				transform: rotate(2deg);
				text-decoration: underline;
				text-decoration-thickness: 8px;
				text-decoration-color: var(--blue);
			}

			.line-3 {
				font-family: 'Black Ops One', cursive;
				color: var(--cyan);
				transform: rotate(-2deg);
				letter-spacing: 0.2rem;
			}

			.subtitle-container {
				margin-bottom: 3rem;
			}

			.subtitle {
				font-family: 'Bangers', cursive;
				font-size: clamp(2rem, 5vw, 3rem);
				line-height: 1.4;
			}

			.highlight-pink {
				background: var(--pink);
				color: var(--yellow);
				padding: 0.2rem 0.8rem;
				transform: rotate(-2deg);
				display: inline-block;
			}

			.highlight-yellow {
				background: var(--yellow);
				color: var(--pink);
				padding: 0.2rem 0.8rem;
				transform: rotate(2deg);
				display: inline-block;
			}

			.highlight-blue {
				background: var(--blue);
				color: var(--yellow);
				padding: 0.2rem 0.8rem;
				transform: rotate(-1deg);
				display: inline-block;
			}

			.cta-cluster {
				display: flex;
				justify-content: center;
				gap: 1.5rem;
				flex-wrap: wrap;
			}

			.btn {
				font-family: 'Bangers', cursive;
				font-size: 1.5rem;
				padding: 1rem 2.5rem;
				border: 4px solid #000;
				cursor: pointer;
				transition: all 0.2s;
				box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.5);
			}

			.btn:hover {
				transform: translate(-4px, -4px);
				box-shadow: 10px 10px 0 rgba(0, 0, 0, 0.5);
			}

			.btn-1 { background: var(--pink); color: var(--yellow); transform: rotate(-3deg); }
			.btn-2 { background: var(--yellow); color: var(--pink); transform: rotate(2deg); }
			.btn-3 { background: var(--blue); color: var(--yellow); transform: rotate(-1deg); }

			/* Features Section */
			.features {
				padding: 6rem 0;
			}

			.section-title {
				text-align: center;
				margin-bottom: 4rem;
				display: flex;
				justify-content: center;
				gap: 1rem;
				flex-wrap: wrap;
			}

			.title-word {
				font-size: clamp(2.5rem, 6vw, 4rem);
				padding: 0.5rem 1.5rem;
				border: 4px solid #000;
				box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.5);
			}

			.word-1 { background: var(--pink); color: var(--yellow); transform: rotate(-2deg); }
			.word-2 { background: var(--yellow); color: var(--pink); transform: rotate(3deg); }
			.word-3 { background: var(--blue); color: var(--yellow); transform: rotate(-1deg); }
			.word-4 { background: var(--cyan); color: var(--pink); transform: rotate(2deg); }

			.features-chaos {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 2rem;
			}

			.feature-box {
				padding: 2rem;
				border: 5px solid #000;
				position: relative;
				transition: all 0.3s;
			}

			.feature-box:hover {
				transform: scale(1.05) rotate(2deg);
			}

			.box-1 { background: var(--pink); color: var(--yellow); }
			.box-2 { background: var(--yellow); color: var(--pink); }
			.box-3 { background: var(--blue); color: var(--yellow); }
			.box-4 { background: var(--cyan); color: var(--pink); }
			.box-5 { background: var(--purple); color: var(--yellow); }
			.box-6 { background: var(--orange); color: #000; }

			.box-header {
				font-family: 'Rubik Mono One', cursive;
				font-size: 3rem;
				opacity: 0.3;
				margin-bottom: 1rem;
			}

			.feature-box h3 {
				font-family: 'Bangers', cursive;
				font-size: 2rem;
				margin-bottom: 1rem;
			}

			.feature-box p {
				font-family: 'Bebas Neue', sans-serif;
				font-size: 1.2rem;
				line-height: 1.6;
			}

			.box-badge {
				position: absolute;
				top: 1rem;
				right: 1rem;
				font-size: 1.5rem;
			}

			/* CTA Section */
			.cta {
				padding: 8rem 0;
				position: relative;
			}

			.cta-background {
				position: absolute;
				inset: 0;
				background:
					repeating-linear-gradient(0deg, var(--pink) 0px, var(--pink) 20px, var(--yellow) 20px, var(--yellow) 40px, var(--blue) 40px, var(--blue) 60px);
				opacity: 0.2;
			}

			.cta-content {
				position: relative;
				z-index: 1;
				text-align: center;
			}

			.cta-badges {
				display: flex;
				justify-content: center;
				gap: 1rem;
				margin-bottom: 2rem;
			}

			.badge {
				font-family: 'Bangers', cursive;
				font-size: 1.5rem;
				padding: 0.5rem 1.5rem;
				border: 4px solid #000;
				box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.5);
			}

			.badge-1 { background: var(--pink); color: var(--yellow); transform: rotate(-5deg); }
			.badge-2 { background: var(--yellow); color: var(--pink); transform: rotate(3deg); }
			.badge-3 { background: var(--blue); color: var(--yellow); transform: rotate(-3deg); }

			.cta-title {
				font-family: 'Black Ops One', cursive;
				font-size: clamp(3rem, 8vw, 6rem);
				margin-bottom: 2rem;
				text-shadow: 8px 8px 0 rgba(0, 0, 0, 0.8);
			}

			.cta-emphasis {
				color: var(--yellow);
			}

			.cta-text {
				font-family: 'Bebas Neue', sans-serif;
				font-size: 2rem;
				margin-bottom: 3rem;
			}

			.cta-button {
				font-family: 'Bangers', cursive;
				font-size: 2.5rem;
				padding: 1.5rem 4rem;
				background: var(--pink);
				color: var(--yellow);
				border: 6px solid #000;
				cursor: pointer;
				position: relative;
				box-shadow: 10px 10px 0 rgba(0, 0, 0, 0.8);
				transition: all 0.2s;
			}

			.cta-button:hover {
				transform: translate(-6px, -6px);
				box-shadow: 16px 16px 0 rgba(0, 0, 0, 0.8);
			}

			.button-effects {
				position: absolute;
				inset: -10px;
				border: 3px dashed var(--cyan);
				animation: dashRotate 10s linear infinite;
			}

			@keyframes dashRotate {
				from { transform: rotate(0deg); }
				to { transform: rotate(360deg); }
			}

			/* Footer */
			.footer {
				padding: 3rem 0;
				position: relative;
			}

			.footer-pattern {
				position: absolute;
				inset: 0;
				background:
					repeating-linear-gradient(45deg, var(--pink) 0, var(--pink) 10px, var(--yellow) 10px, var(--yellow) 20px, var(--blue) 20px, var(--blue) 30px);
				opacity: 0.3;
			}

			.footer-text {
				position: relative;
				z-index: 1;
				text-align: center;
				font-size: 1rem;
				letter-spacing: 0.2rem;
			}

			/* Responsive */
			@media (max-width: 768px) {
				.sticker-collection {
					flex-direction: column;
					align-items: center;
					gap: 1rem;
				}

				.cta-cluster {
					flex-direction: column;
					align-items: center;
				}

				.section-title {
					flex-direction: column;
					align-items: center;
				}

				.features-chaos {
					grid-template-columns: 1fr;
				}

				.cta-badges {
					flex-direction: column;
					align-items: center;
				}
			}
		</style>
	</body>
</html>
