---
import Header from '../../soft-sweet-dreams-site/components/Header.astro';
import Footer from '../../soft-sweet-dreams-site/components/Footer.astro';

const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Services - Sweet Dreams</title>
  <meta name="description" content="Sweet Dreamsが提供する夢のようなデザインサービス" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&family=Nunito:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href={`${base}soft-sweet-dreams-site/styles/tokens.css`} />
</head>
<body>
  <Header />

  <!-- Floating Background -->
  <div class="dream-bg">
    <div class="cloud cloud-1"></div>
    <div class="cloud cloud-2"></div>
    <div class="cloud cloud-3"></div>
  </div>

  <!-- Page Hero -->
  <section class="page-hero">
    <div class="container">
      <h1>Our Services</h1>
      <p>あなたのビジョンを夢のような体験に変えます</p>
    </div>
  </section>

  <!-- Services Grid -->
  <section class="services-section">
    <div class="container">
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">🎨</div>
          <h3>Branding</h3>
          <p class="service-description">
            ブランドの世界観を創造し、
            記憶に残る独自のアイデンティティを構築します。
          </p>
          <ul class="service-features">
            <li>ロゴデザイン</li>
            <li>ブランドガイドライン</li>
            <li>カラーパレット設計</li>
            <li>タイポグラフィシステム</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">💻</div>
          <h3>Web Design</h3>
          <p class="service-description">
            レスポンシブで美しいWebサイトを設計・制作。
            ユーザーを魅了する体験を提供します。
          </p>
          <ul class="service-features">
            <li>Webサイトデザイン</li>
            <li>ランディングページ制作</li>
            <li>レスポンシブ対応</li>
            <li>アニメーション実装</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">📱</div>
          <h3>UI/UX Design</h3>
          <p class="service-description">
            ユーザー中心の設計で、
            直感的で心地よいインターフェースを実現します。
          </p>
          <ul class="service-features">
            <li>ユーザーリサーチ</li>
            <li>ワイヤーフレーム作成</li>
            <li>プロトタイプ制作</li>
            <li>ユーザビリティテスト</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">🎭</div>
          <h3>Motion Design</h3>
          <p class="service-description">
            軽やかなアニメーションとインタラクションで、
            体験に命を吹き込みます。
          </p>
          <ul class="service-features">
            <li>マイクロインタラクション</li>
            <li>ページトランジション</li>
            <li>スクロールアニメーション</li>
            <li>ローディング演出</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">🖼️</div>
          <h3>Illustration</h3>
          <p class="service-description">
            独自のイラストレーションで、
            ブランドに個性と温かみを添えます。
          </p>
          <ul class="service-features">
            <li>カスタムイラスト</li>
            <li>アイコンセット</li>
            <li>キャラクターデザイン</li>
            <li>パターンデザイン</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">🛠️</div>
          <h3>Development</h3>
          <p class="service-description">
            最新技術でデザインを実装。
            パフォーマンスと品質にこだわります。
          </p>
          <ul class="service-features">
            <li>フロントエンド開発</li>
            <li>レスポンシブコーディング</li>
            <li>パフォーマンス最適化</li>
            <li>アクセシビリティ対応</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing Section -->
  <section class="pricing-section">
    <div class="container">
      <div class="section-header">
        <h2>Pricing Plans</h2>
        <p>あなたに最適なプランを選択してください</p>
      </div>

      <div class="pricing-grid">
        <!-- Starter Plan -->
        <div class="pricing-card">
          <div class="plan-badge">Starter</div>
          <div class="plan-icon">🌸</div>
          <h3 class="plan-name">スターター</h3>
          <div class="plan-price">
            <span class="currency">¥</span>
            <span class="amount">300,000</span>
            <span class="period">〜</span>
          </div>
          <p class="plan-description">
            小規模プロジェクトに最適
          </p>
          <ul class="plan-features">
            <li>✨ ランディングページ制作</li>
            <li>✨ レスポンシブ対応</li>
            <li>✨ 基本アニメーション</li>
            <li>✨ 1ヶ月サポート</li>
          </ul>
          <a href={`${base}soft-sweet-dreams-site/contact`} class="btn btn-secondary btn-full">
            選択する
          </a>
        </div>

        <!-- Professional Plan (Featured) -->
        <div class="pricing-card featured">
          <div class="plan-badge popular">Popular</div>
          <div class="plan-icon">☁️</div>
          <h3 class="plan-name">プロフェッショナル</h3>
          <div class="plan-price">
            <span class="currency">¥</span>
            <span class="amount">800,000</span>
            <span class="period">〜</span>
          </div>
          <p class="plan-description">
            中規模プロジェクトに最適
          </p>
          <ul class="plan-features">
            <li>✨ 完全なWebサイト制作</li>
            <li>✨ カスタムデザイン</li>
            <li>✨ 高度なアニメーション</li>
            <li>✨ UI/UX設計</li>
            <li>✨ 3ヶ月サポート</li>
          </ul>
          <a href={`${base}soft-sweet-dreams-site/contact`} class="btn btn-primary btn-full">
            選択する
          </a>
        </div>

        <!-- Enterprise Plan -->
        <div class="pricing-card">
          <div class="plan-badge">Enterprise</div>
          <div class="plan-icon">✨</div>
          <h3 class="plan-name">エンタープライズ</h3>
          <div class="plan-price">
            <span class="currency">¥</span>
            <span class="amount">2,000,000</span>
            <span class="period">〜</span>
          </div>
          <p class="plan-description">
            大規模プロジェクトに最適
          </p>
          <ul class="plan-features">
            <li>✨ フルスケールのWebシステム</li>
            <li>✨ ブランディング全体</li>
            <li>✨ カスタムイラスト</li>
            <li>✨ 高度なインタラクション</li>
            <li>✨ 6ヶ月サポート</li>
            <li>✨ 専任担当者</li>
          </ul>
          <a href={`${base}soft-sweet-dreams-site/contact`} class="btn btn-secondary btn-full">
            選択する
          </a>
        </div>
      </div>

      <div class="pricing-note">
        <p>※ 価格は目安です。プロジェクトの規模や内容により変動します。</p>
        <p>お気軽にお問い合わせください。</p>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>プロジェクトを始めましょう</h2>
        <p>あなたのビジョンをお聞かせください</p>
        <a href={`${base}soft-sweet-dreams-site/contact`} class="btn btn-primary btn-large">
          お問い合わせ
        </a>
        <div class="cta-links">
          <a href={`${base}soft-sweet-dreams-site/about`} class="text-link">
            チームについてもっと知る →
          </a>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</body>
</html>

<style>
  body {
    background: linear-gradient(135deg,
      var(--dream-pink) 0%,
      var(--dream-lavender) 30%,
      var(--dream-mint) 60%,
      var(--dream-cream) 100%);
    min-height: 100vh;
  }

  /* Floating Background */
  .dream-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
  }

  .cloud {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.3;
    animation: float 20s ease-in-out infinite;
  }

  .cloud-1 {
    width: 350px;
    height: 350px;
    background: var(--dream-lavender);
    top: 15%;
    right: 10%;
  }

  .cloud-2 {
    width: 400px;
    height: 400px;
    background: var(--dream-mint);
    top: 60%;
    left: 10%;
    animation-delay: 7s;
  }

  .cloud-3 {
    width: 300px;
    height: 300px;
    background: var(--dream-peach);
    bottom: 10%;
    right: 20%;
    animation-delay: 14s;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    25% {
      transform: translate(20px, -20px) scale(1.1);
    }
    50% {
      transform: translate(-15px, -40px) scale(0.9);
    }
    75% {
      transform: translate(10px, -20px) scale(1.05);
    }
  }

  /* Page Hero */
  .page-hero {
    position: relative;
    z-index: 1;
    padding: 10rem 0 4rem;
    text-align: center;
  }

  .page-hero h1 {
    font-family: var(--font-display);
    font-size: var(--fs-5xl);
    font-weight: 700;
    background: linear-gradient(135deg,
      var(--dream-deep-purple),
      var(--dream-accent-1),
      var(--dream-accent-2));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }

  .page-hero p {
    font-family: var(--font-body);
    font-size: var(--fs-xl);
    color: var(--dream-purple);
  }

  /* Services Section */
  .services-section {
    position: relative;
    z-index: 1;
    padding: 4rem 0;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .service-card {
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    border-radius: var(--radius-lg);
    padding: 3rem 2.5rem;
    border: 2px solid var(--glass-border);
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-lg);
  }

  .service-icon {
    font-size: 4rem;
    margin-bottom: 1.5rem;
  }

  .service-card h3 {
    font-family: var(--font-display);
    font-size: var(--fs-xl);
    font-weight: 700;
    color: var(--dream-deep-purple);
    margin-bottom: 1rem;
  }

  .service-description {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    color: var(--dream-purple);
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .service-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .service-features li {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    color: var(--dream-purple);
    padding-left: 1.5rem;
    position: relative;
  }

  .service-features li::before {
    content: '✨';
    position: absolute;
    left: 0;
  }

  /* Pricing Section */
  .pricing-section {
    position: relative;
    z-index: 1;
    padding: 6rem 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-family: var(--font-display);
    font-size: var(--fs-4xl);
    font-weight: 700;
    color: var(--dream-deep-purple);
    margin-bottom: 1rem;
  }

  .section-header p {
    font-family: var(--font-body);
    font-size: var(--fs-lg);
    color: var(--dream-purple);
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .pricing-card {
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    border-radius: var(--radius-lg);
    padding: 3rem 2.5rem;
    border: 2px solid var(--glass-border);
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    position: relative;
  }

  .pricing-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-lg);
  }

  .pricing-card.featured {
    border: 3px solid var(--dream-accent-1);
    box-shadow: var(--shadow-lg), 0 0 30px rgba(192, 132, 252, 0.3);
  }

  .plan-badge {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    padding: 0.5rem 1rem;
    background: rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(10px);
    border-radius: var(--radius-full);
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--dream-purple);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .plan-badge.popular {
    background: linear-gradient(135deg, var(--dream-accent-1), var(--dream-accent-2));
    color: var(--dream-white);
  }

  .plan-icon {
    font-size: 4rem;
    margin-bottom: 1.5rem;
  }

  .plan-name {
    font-family: var(--font-display);
    font-size: var(--fs-xl);
    font-weight: 700;
    color: var(--dream-deep-purple);
    margin-bottom: 1rem;
  }

  .plan-price {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .currency {
    font-family: var(--font-display);
    font-size: var(--fs-lg);
    color: var(--dream-purple);
  }

  .amount {
    font-family: var(--font-display);
    font-size: var(--fs-4xl);
    font-weight: 700;
    color: var(--dream-deep-purple);
  }

  .period {
    font-family: var(--font-body);
    font-size: var(--fs-base);
    color: var(--dream-purple);
  }

  .plan-description {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    color: var(--dream-purple);
    margin-bottom: 2rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(139, 126, 159, 0.2);
  }

  .plan-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 2.5rem;
    flex-grow: 1;
  }

  .plan-features li {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    color: var(--dream-purple);
    line-height: 1.6;
  }

  .btn-full {
    width: 100%;
    text-align: center;
  }

  .pricing-note {
    text-align: center;
    margin-top: 2rem;
  }

  .pricing-note p {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    color: var(--dream-purple);
    line-height: 1.6;
  }

  /* CTA Section */
  .cta-section {
    position: relative;
    z-index: 1;
    padding: 6rem 0;
  }

  .cta-content {
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    border-radius: var(--radius-xl);
    padding: 4rem 3rem;
    border: 2px solid var(--glass-border);
    box-shadow: var(--shadow-lg);
    text-align: center;
  }

  .cta-content h2 {
    font-family: var(--font-display);
    font-size: var(--fs-4xl);
    font-weight: 700;
    color: var(--dream-deep-purple);
    margin-bottom: 1rem;
  }

  .cta-content p {
    font-family: var(--font-body);
    font-size: var(--fs-lg);
    color: var(--dream-purple);
    margin-bottom: 2.5rem;
  }

  .btn-large {
    padding: 1.25rem 3rem;
    font-size: var(--fs-lg);
  }

  .cta-links {
    margin-top: 2rem;
  }

  .text-link {
    font-family: var(--font-body);
    font-size: var(--fs-base);
    color: var(--dream-deep-purple);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    display: inline-block;
  }

  .text-link:hover {
    color: var(--dream-accent-1);
    transform: translateX(5px);
  }

  /* Responsive Styles */
  @media (max-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .pricing-grid {
      grid-template-columns: 1fr;
      max-width: 500px;
      margin-left: auto;
      margin-right: auto;
    }
  }

  @media (max-width: 768px) {
    .page-hero {
      padding: 8rem 0 3rem;
    }

    .page-hero h1 {
      font-size: var(--fs-4xl);
    }

    .page-hero p {
      font-size: var(--fs-base);
    }

    .services-section,
    .pricing-section,
    .cta-section {
      padding: 4rem 0;
    }

    .services-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .service-card {
      padding: 2.5rem 2rem;
    }

    .section-header h2,
    .cta-content h2 {
      font-size: var(--fs-3xl);
    }

    .cta-content {
      padding: 3rem 2rem;
    }

    .btn-large {
      width: 100%;
    }

    .cloud-1, .cloud-2, .cloud-3 {
      width: 200px;
      height: 200px;
    }
  }

  @media (max-width: 480px) {
    .page-hero h1 {
      font-size: var(--fs-3xl);
    }

    .service-icon {
      font-size: 3rem;
    }

    .plan-icon {
      font-size: 3rem;
    }

    .amount {
      font-size: var(--fs-3xl);
    }

    .pricing-card {
      padding: 2.5rem 2rem;
    }
  }
</style>
